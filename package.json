{
  "name": "tribunal",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "applications/*",
    "packages/*",
    "runner",
    "scripts"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "check": "turbo run check",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "test:coverage:review-engine": "bun run --cwd applications/engine test:coverage && bun run --cwd applications/proxy test:coverage && bun run --cwd packages/review-core test:coverage && bun run --cwd packages/agents test:coverage && bun run --cwd packages/sandbox test:coverage && bun run --cwd runner test && bun run --cwd packages/cost test:coverage && bun run --cwd packages/github test:coverage:review-engine",
    "prepare": "bun run scripts/install-git-hooks.ts",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "precommit:tests": "bun run scripts/precommit-tests.ts",
    "db:generate": "cd packages/database && bunx drizzle-kit generate",
    "db:migrate": "cd packages/database && bunx drizzle-kit migrate",
    "db:check": "cd packages/database && bunx drizzle-kit check",
    "db:studio": "drizzle-kit studio --config packages/database/drizzle.config.ts",
    "database:migration:prepare": "bun run db:generate && bun run db:check && bun run check",
    "verify": "bun run scripts/verify.ts",
    "deploy:status": "bun run scripts/deploy.ts",
    "test:coverage": "turbo run build --filter='!@tribunal/web' && bun run --cwd applications/engine test:coverage && bun run --cwd applications/proxy test:coverage && bun run --cwd applications/web test:coverage && bun run --cwd packages/review-core test:coverage && bun run --cwd packages/agents test:coverage && bun run --cwd packages/sandbox test:coverage && bun run --cwd packages/cost test:coverage && bun run --cwd packages/github test:coverage && bun run --cwd packages/database test:coverage && bun run --cwd packages/test test:coverage && bun run --cwd scripts test:coverage && bun run --cwd runner test"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.2",
    "@vitest/coverage-istanbul": "4.0.16",
    "@vitest/coverage-v8": "4.0.16",
    "bun-types": "^1.3.4",
    "dotenv": "^17.2.3",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-oxlint": "^1.48.0",
    "eslint-plugin-svelte": "^3.13.1",
    "globals": "^16.5.0",
    "lefthook": "^1.10.10",
    "lint-staged": "^16.2.7",
    "oxlint": "^1.48.0",
    "prettier": "^3.7.4",
    "prettier-plugin-svelte": "^3.4.1",
    "svelte": "5.56.4",
    "turbo": "^2.5.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.50.0",
    "vitest": "^4.0.16"
  },
  "lint-staged": {
    "*.{js,ts,svelte,json,md,css,html}": [
      "prettier --write"
    ]
  },
  "overrides": {
    "esrap": "^2.2.1",
    "lucide-svelte": "0.561.0"
  }
}
