{
  "name": "@tribunal/review-core",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./capability-token": "./src/capability-token.ts",
    "./redaction": "./src/redaction.ts",
    "./tokens": "./src/tokens.ts",
    "./schemas": "./src/schemas.ts",
    "./types": "./src/types.ts",
    "./ports": "./src/ports.ts"
  },
  "scripts": {
    "build": "tsc",
    "check": "tsc --noEmit",
    "test": "vitest run --config vitest.configuration.ts",
    "test:coverage": "vitest run --config vitest.configuration.ts --coverage",
    "lint": "oxlint . && eslint .",
    "format": "prettier --write --ignore-path ../../.prettierignore .",
    "format:check": "prettier --check --ignore-path ../../.prettierignore ."
  },
  "dependencies": {
    "zod": "^4.2.1"
  },
  "devDependencies": {
    "@tribunal/typescript": "workspace:*",
    "@types/node": "^25",
    "bun-types": "^1.3.4",
    "vitest": "^4.0.16"
  }
}
