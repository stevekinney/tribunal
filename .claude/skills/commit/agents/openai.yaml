interface:
  display_name: "Commit"
  short_description: "Help with Commit tasks"
  default_prompt: "Use $commit to handle this task with the skill workflow."

policy:
  allow_implicit_invocation: false
