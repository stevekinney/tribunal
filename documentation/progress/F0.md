# F0 Foundation

Status: complete

## Done

- Added buildable workspace skeletons for `applications/engine`, `applications/proxy`, and `packages/review-core`.
- Added engine and proxy environment parsers with tests for full fixtures and missing required variables.
- Added `/health` response handlers for engine and proxy with unit tests.
- Added shared opaque-token helpers and tests that avoid token prefix or length assumptions.
- Ran `bun install`, `bun run build`, and `bun run verify`; all passed.

## Left

- None.

## Failures

- None yet.

<promise>TRACK_F0_DONE</promise>
