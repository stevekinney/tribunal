# Settings Matrix

This inventory traces each current user-facing user, repository, and agent setting from the
operator control to persisted state, runtime reader, observable effect, and regression evidence.
It was produced for GitHub issue #202 against `origin/main` at
`c6b323a74b71cccba7a7b5ff42d241019052d6ec`.

## Scope

Included:

- Global review settings on `/settings`.
- Repository review controls on `/repositories`, `/onboarding`, and
  `/repositories/[repositoryId]/settings`.
- Repository event listener controls on `/repositories/[repositoryId]/events`.
- Agent list, create, and edit controls on `/agents`, `/agents/new`, and `/agents/[agentId]`.

Excluded:

- Query-string filters on list pages because they are not persisted settings.
- Stop-run and stop-agent controls because they mutate run state, not review configuration.
- `repository_review_settings.check_conclusion_mode` because it is a persisted engine setting, but
  no current user-facing control writes it. The engine path is wired and covered by tests; exposing
  it would be new product surface, not a no-op fix.

## Verdict Summary

| Area       | Setting controls audited | Wired | Partial | Silent no-op |
| ---------- | ------------------------ | ----- | ------- | ------------ |
| User       | 3                        | 3     | 0       | 0            |
| Repository | 9                        | 9     | 0       | 0            |
| Agent      | 7                        | 6     | 1       | 0            |

Confirmed follow-ups:

- [#229](https://github.com/stevekinney/tribunal/issues/229): users with watched repositories
  created before the centralized future-write invariant need a data-only phase-2 backfill after
  phase 1 deploys successfully.
- [#231](https://github.com/stevekinney/tribunal/issues/231): deleting an agent now preserves
  historical specialist run rows, findings, and events while clearing the live agent reference.

Live dedupe checks covered open issues #202-#209 and #223-#224, recent closed issues, and targeted
open/closed searches for user review settings seeding, disabled-agent assignment fallback, and agent
delete history cascade behavior before filing #229-#231.

## User Settings

| Control                                                                                                                                                                                                                     | Persisted field                                                                                       | Runtime reader                                                                                                                                                                                                                                                                                                                           | Observable effect                                                                                                                                                                                                                                                                                                                                                       | Evidence                                                                                                                                                                                                                                                                                                                                                                    | Verdict |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| `/settings` daily cost cap input (`dailyCostCapUsd`) in `applications/web/src/routes/(authenticated)/settings/+page.svelte`; saved through `saveUserReviewSettings` in `applications/web/src/lib/server/review/operator.ts` | `user_review_settings.daily_cost_cap_usd` from `packages/database/src/schema/user-review-settings.ts` | `readDailyCostCap` and `enforceDailyCap` in `packages/cost/src/ledger.ts`; engine calls `ports.cost.enforceDailyCap` before the run, reserves budget before triage, each specialist, and each verifier in `applications/engine/src/workflows/review-workflow.ts`                                                                         | A run becomes `quota_blocked` and the Tribunal check run is completed as neutral when estimated daily spend plus active reservations reaches the cap. Reservations can block new LLM work before recorded spend reaches the cap; completed estimate writes release the matching reservation. The Costs page also displays the configured cap through `getCostOverview`. | `applications/web/src/lib/server/review/operator.test.ts` covers validation and upsert/readback. `packages/cost/src/ledger.test.ts` covers cap enforcement, atomic reservations, retry refresh, and fallback defaults. `applications/engine/src/workflows/review-workflow.test.ts` covers pre-run, triage, specialist, verifier, and cancellation quota paths.              | Wired   |
| `/settings` default model select (`defaultModel`) in `applications/web/src/routes/(authenticated)/settings/+page.svelte`; options come from `getDefaultModelOptions` and save validation uses `defaultReviewModelSchema`    | `user_review_settings.default_model`                                                                  | `buildPullRequestReviewInput` in `applications/engine/src/workflows/review-intent-port.ts` selects `userReviewSettings.defaultModel`; `ReviewWorkflowEngine.resolveDefaultModel` in `applications/engine/src/workflows/review-workflow.ts` validates it and passes it into `toAgentDefinition` from `packages/agents/src/definitions.ts` | Agents whose own model is `inherit` run with the stored user default. Invalid stored values fall back to `TRIBUNAL_DEFAULT_MODEL` instead of reaching the sandbox.                                                                                                                                                                                                      | `applications/web/src/lib/server/review/operator.test.ts` rejects raw and inherited user defaults. `applications/engine/src/workflows/review-intent-port.test.ts` carries `defaultModel` onto claimed input. `applications/engine/src/workflows/review-workflow-default-model.test.ts` verifies the value reaches sandbox calls and does not drift across supervisor races. | Wired   |
| `/settings` kill switch toggle (`reviewsEnabled`) in `applications/web/src/routes/(authenticated)/settings/+page.svelte`; saved through `saveUserReviewSettings`                                                            | `user_review_settings.reviews_enabled`                                                                | `claimNextIntentRow`, `getReviewIntentQueueStatus`, and `buildPullRequestReviewInput` in `applications/engine/src/workflows/review-intent-port.ts`; `getReviewsEnabled` in `applications/web/src/lib/server/review/operator.ts` feeds authenticated layout status                                                                        | Disabled users have zero claimable review intents and queued intents are not built into review input. The authenticated shell can show paused review status without creating a settings row.                                                                                                                                                                            | `applications/web/src/lib/server/review/operator.test.ts` covers pure read behavior. `applications/engine/src/workflows/review-intent-port.test.ts` covers disabled users, watched repositories, and queue status. `applications/web/src/routes/(authenticated)/settings/settings-page.svelte.test.ts` covers the confirmation and unsaved-change UI.                       | Wired   |

## Repository Settings

| Control                                                                                                                                                                                                                                                     | Persisted field                                                                                     | Runtime reader                                                                                                                                                                                                                                                                                                                         | Observable effect                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | Verdict |
| ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------- |
| Repository add/watch controls on `/repositories` and onboarding selection in `applications/web/src/routes/(authenticated)/repositories/+page.svelte` and `applications/web/src/routes/onboarding/+page.svelte`; saved through `saveRepositoryWatchSettings` | `repository_review_settings.watched`; `user_review_settings` is seeded when saving `watched = true` | `enqueuePullRequestReviewIntent` in `packages/github/src/pull-requests/state/workflow-signals.ts`, plus `claimNextIntentRow`, `getReviewIntentQueueStatus`, and `buildPullRequestReviewInput` in `applications/engine/src/workflows/review-intent-port.ts`, all require a watched repository and an enabled `user_review_settings` row | Watched repositories are eligible for automated review intent fanout and claiming. Unwatched repositories remain visible to users with GitHub access but do not dispatch reviews. Every future watch path now ensures schema-default user review settings without overwriting existing user settings. Existing watched users who predate this invariant remain tracked by follow-up [#229](https://github.com/stevekinney/tribunal/issues/229) for a phase-2 data-only backfill after phase 1 deploys. | `applications/web/src/routes/(authenticated)/repositories/server.spec.ts` covers add/re-add behavior. `applications/web/src/routes/onboarding/onboarding-page.server.test.ts` covers first-time watch seeding. `applications/web/src/lib/server/review/operator.test.ts` covers centralized user-setting seeding and existing-setting preservation for direct saves and the repository settings form path. `applications/engine/src/workflows/review-intent-port.test.ts` covers watched/unwatched eligibility.                                                                                  | Wired   |
| Repository ignore globs tag input (`ignoreGlobs`) on `/repositories/[repositoryId]/settings` in `applications/web/src/routes/(authenticated)/repositories/[repositoryId=int]/settings/+page.svelte`; saved through `submitRepositorySettingsForm`           | `repository_review_settings.ignore_globs`                                                           | `buildPullRequestReviewInput` in `applications/engine/src/workflows/review-intent-port.ts` selects `repositoryReviewSettings.ignoreGlobs`; `ReviewWorkflowEngine.startPullRequestReview` unions it with `DEFAULT_IGNORE_GLOBS` before `shouldSkipIgnoredDiff` in `applications/engine/src/workflows/review-workflow.ts`                | Reviews are skipped with a successful check run when every changed file matches configured ignore globs.                                                                                                                                                                                                                                                                                                                                                                                               | `applications/web/src/lib/server/review/operator.test.ts` covers normalization, deduplication, and readback. `applications/web/src/routes/(authenticated)/repositories/[repositoryId=int]/settings/settings-page.svelte.test.ts` covers submitted tag values. `applications/engine/src/workflows/review-workflow.test.ts` covers ignored-diff skip behavior.                                                                                                                                                                                                                                     | Wired   |
| Repository review agent assignment checkboxes (`agentIds`) on `/repositories/[repositoryId]/settings`; first-time add/onboarding defaults use enabled agents                                                                                                | `repository_agent.user_id`, `repository_agent.repository_id`, `repository_agent.agent_id`           | `buildPullRequestReviewInput` reads repository assignment rows, filters enabled assigned agents, and falls back to `selectEnabledUserAgents` only when no repository assignment rows exist in `applications/engine/src/workflows/review-intent-port.ts`                                                                                | Assigned enabled agents determine the specialist agent set for repository reviews. Repositories with no explicit assignments use all enabled user agents. Explicit assignment rows with only disabled agents wait for an eligible assigned agent instead of widening to unrelated agents.                                                                                                                                                                                                              | `applications/web/src/lib/server/review/operator.test.ts` covers authorization, atomic settings/assignment writes, duplicate handling, per-user isolation, and engine kicks when enabling agents. `applications/web/src/routes/(authenticated)/repositories/[repositoryId=int]/settings/settings-page.svelte.test.ts` covers disabled-agent assignment display. `applications/engine/src/workflows/review-intent-port.test.ts` covers assigned-agent selection, disabled-agent deferral, fallback only without assignment rows, queue status idleness, and continuing to later eligible intents. | Wired   |
| Event listener name input (`name`) on `/repositories/[repositoryId]/events` in `applications/web/src/routes/(authenticated)/repositories/[repositoryId=int]/events/event-listener-form.svelte`; saved through create/update actions                         | `repository_event_listener.name`                                                                    | `listEventListenersWithProgressForRepository` displays it in the listener list; matching and dispatch use the listener id rather than the name                                                                                                                                                                                         | Renaming changes the operator-facing listener label without changing match behavior. Blank names are rejected at the route and schema boundary.                                                                                                                                                                                                                                                                                                                                                        | `applications/web/src/routes/(authenticated)/repositories/[repositoryId=int]/events/server.spec.ts` covers create/update validation. `packages/database/src/queries/__tests__/event-listeners.test.ts` covers persistence and blank-name constraints.                                                                                                                                                                                                                                                                                                                                            | Wired   |
| Event listener event type and action selects (`eventType`, `action`) on `/repositories/[repositoryId]/events`                                                                                                                                               | `repository_event_listener.event_type`, `repository_event_listener.action`                          | `listEnabledListenersForRepositoryEventType` preselects enabled listeners by repository and event type; `eventListenerMatchesEvent` in `packages/github/src/webhooks/event-listener-matching.ts` re-checks event type and optional action                                                                                              | Matching webhook events create pending `event_listener_delivery` rows only for listeners with the same event type and, when set, the same action.                                                                                                                                                                                                                                                                                                                                                      | `packages/github/src/webhooks/event-listener-matching.test.ts` covers type/action matching and redelivery behavior. `packages/database/src/queries/__tests__/event-listeners.test.ts` covers list queries scoped by event type.                                                                                                                                                                                                                                                                                                                                                                  | Wired   |
| Event listener filters (`filterRef`, `filterSenderLogin`, `filterPrNumber`, `filterIssueNumber`) on `/repositories/[repositoryId]/events`                                                                                                                   | `repository_event_listener.filters_json`                                                            | `parseEventListenerFilters` and `eventListenerMatchesEvent` compare stored filters to normalized `webhook_event` fields (`ref`, `sender_login`, `pr_number`, `issue_number`)                                                                                                                                                           | A listener fires only when every stored filter matches the webhook event. Malformed stored filters fail closed and the edit form requires explicit acknowledgement before replacing them.                                                                                                                                                                                                                                                                                                              | `applications/web/src/routes/(authenticated)/repositories/[repositoryId=int]/events/event-listener-form.svelte.test.ts` covers form state. `packages/database/src/queries/__tests__/event-listener-filters.test.ts` covers serialization/parsing. `packages/github/src/webhooks/event-listener-matching.test.ts` covers filter matching and fail-closed behavior.                                                                                                                                                                                                                                | Wired   |
| Event listener agent select (`agentId`) on `/repositories/[repositoryId]/events`                                                                                                                                                                            | `repository_event_listener.agent_id`                                                                | `listEnabledListenersForRepositoryEventType` joins `agent.enabled = true` at match time; `dispatchClaimedDelivery` in `packages/github/src/webhooks/event-listener-dispatch.ts` re-checks listener ownership, installation access, agent existence, and `agent.enabled` before inserting `agent_run`                                   | Matching events dispatch the selected agent. Disabled selected agents stop new matches, and a listener or agent disabled after matching causes the delivery to be marked failed/disabled rather than running stale work.                                                                                                                                                                                                                                                                               | `packages/database/src/queries/__tests__/event-listeners.test.ts` covers agent ownership. `packages/github/src/webhooks/event-listener-dispatch.test.ts` covers disabled listener/agent dispatch handling.                                                                                                                                                                                                                                                                                                                                                                                       | Wired   |
| Event listener instructions editor (`instructionsMarkdown`) on `/repositories/[repositoryId]/events`                                                                                                                                                        | `repository_event_listener.instructions_markdown`                                                   | `dispatchClaimedDelivery` passes it into `buildEventListenerPrompt` in `packages/github/src/webhooks/event-listener-prompt.ts` before creating the webhook handler run                                                                                                                                                                 | The selected agent's event-listener prompt includes the stored instructions when the delivery dispatches.                                                                                                                                                                                                                                                                                                                                                                                              | `applications/web/src/routes/(authenticated)/repositories/[repositoryId=int]/events/server.spec.ts` covers form submission. `packages/github/src/webhooks/event-listener-prompt.test.ts` covers prompt inclusion/omission.                                                                                                                                                                                                                                                                                                                                                                       | Wired   |
| Event listener enabled toggle on `/repositories/[repositoryId]/events` list and create/edit form                                                                                                                                                            | `repository_event_listener.enabled`                                                                 | `listEnabledListenersForRepositoryEventType` filters enabled listeners at match time; `dispatchClaimedDelivery` re-checks `listener.enabled` after claim                                                                                                                                                                               | Disabled listeners do not match new webhook events, and listeners disabled after a delivery was matched stop at dispatch with disabled delivery bookkeeping.                                                                                                                                                                                                                                                                                                                                           | `applications/web/src/routes/(authenticated)/repositories/[repositoryId=int]/events/events-page.svelte.test.ts` covers the toggle UI. `packages/database/src/queries/__tests__/event-listeners.test.ts` covers `setEventListenerEnabled`. `packages/github/src/webhooks/event-listener-dispatch.test.ts` covers dispatch-time disabled handling.                                                                                                                                                                                                                                                 | Wired   |

## Agent Settings

| Control                                                                                                         | Persisted field                                                                                                                                                | Runtime reader                                                                                                                                                                                                                                                                            | Observable effect                                                                                                                                                                                             | Evidence                                                                                                                                                                                                                                                                                                                                                                                                                          | Verdict |
| --------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- |
| Agent enabled toggle on `/agents`, header checkbox on `/agents/new`, and header checkbox on `/agents/[agentId]` | `agent.enabled`                                                                                                                                                | `buildPullRequestReviewInput` filters assigned agents with `agent.enabled = true`; `selectEnabledUserAgents` also filters enabled agents in `applications/engine/src/workflows/review-intent-port.ts`                                                                                     | Disabled agents do not run for repository automation. Repository settings still show selected disabled agents so users can remove them.                                                                       | `applications/web/src/routes/(authenticated)/agents/agents-page.svelte.test.ts` covers list toggle submission. `applications/web/src/routes/(authenticated)/agents/new/new-agent-page.svelte.test.ts` and `applications/web/src/routes/(authenticated)/agents/[agentId]/agent-detail-page.svelte.test.ts` cover form association. `applications/engine/src/workflows/review-intent-port.test.ts` covers disabled-agent exclusion. | Wired   |
| Agent slug input (`slug`) in `applications/web/src/routes/(authenticated)/agents/agent-editor.svelte`           | `agent.slug`                                                                                                                                                   | `toAgentSpec` in `applications/engine/src/workflows/review-intent-port.ts`; `ReviewWorkflowEngine` uses the slug in agent run identifiers, triage available-agent context, sandbox agent key, check-run summaries, and pull request review output                                         | The slug identifies the agent in runtime execution, status summaries, and posted review comments.                                                                                                             | `packages/review-core/src/schemas.ts` validates slug shape through `agentSpecSchema`. `applications/web/src/lib/server/review/operator.test.ts` covers agent persistence. `packages/agents/src/definitions.test.ts` covers mapping to an agent definition key.                                                                                                                                                                    | Wired   |
| Agent description input (`description`) in `agent-editor.svelte`                                                | `agent.description`                                                                                                                                            | `toAgentSpec` in `review-intent-port.ts`; `toAgentDefinition` in `packages/agents/src/definitions.ts` maps it to the Claude Agent SDK agent description; run inspector reads it through `getRunInspector` in `operator.ts`                                                                | The reviewer definition gets the stored description, and run inspector rows display it alongside agent runs.                                                                                                  | `applications/web/src/routes/(authenticated)/agents/agent-editor.svelte.test.ts` covers form rendering and preservation. `packages/agents/src/definitions.test.ts` covers definition mapping.                                                                                                                                                                                                                                     | Wired   |
| Agent system prompt editor (`body`) in `agent-editor.svelte`                                                    | `agent.body`                                                                                                                                                   | `toAgentSpec` in `review-intent-port.ts`; `toAgentDefinition` maps it to `definition.prompt` in `packages/agents/src/definitions.ts`                                                                                                                                                      | The sandbox reviewer receives the stored system prompt as its agent prompt.                                                                                                                                   | `applications/web/src/routes/(authenticated)/agents/agent-editor.svelte.test.ts` verifies edited markdown is submitted through `body`. `packages/agents/src/definitions.test.ts` verifies prompt mapping.                                                                                                                                                                                                                         | Wired   |
| Agent model select (`model`) in `agent-editor.svelte`                                                           | `agent.model`                                                                                                                                                  | `toAgentSpec` in `review-intent-port.ts`; `ReviewWorkflowEngine.runAgentReview` resolves `inherit` through `toAgentDefinition` and forwards the effective model to `executeSandboxAgent`; `packages/sandbox/src/port.ts` sets `TRIBUNAL_AGENT_MODEL`                                      | Explicit agent models override the user default. `inherit` agents use the user default. The sandbox process sees the effective model.                                                                         | `packages/review-core/src/schemas.test.ts` covers accepted model values. `packages/agents/src/definitions.test.ts` covers inherit resolution. `applications/engine/src/workflows/review-workflow-default-model.test.ts` verifies inherited defaults reach sandbox calls. `packages/sandbox/src/port.test.ts` covers model environment forwarding.                                                                                 | Wired   |
| Agent effort select (`effort`) in `agent-editor.svelte`                                                         | `agent.effort`                                                                                                                                                 | `toAgentSpec` in `review-intent-port.ts`; `toAgentDefinition` maps effort, drops effort for Haiku, and falls back `xhigh` to `high` for models that do not support extra-high effort; `packages/sandbox/src/port.ts` sets `TRIBUNAL_AGENT_EFFORT` only when present                       | Specialist runs use the configured effort when the model supports it, omit effort for Haiku, and record the effective effort in agent run output.                                                             | `applications/web/src/routes/(authenticated)/agents/agent-editor.svelte.test.ts` covers fallback notices. `packages/agents/src/definitions.test.ts` covers effort resolution. `applications/engine/src/workflows/review-workflow.test.ts` covers sandbox calls and persisted effective effort. `packages/sandbox/src/port.test.ts` covers effort environment forwarding.                                                          | Wired   |
| Agent delete action on `/agents/[agentId]`                                                                      | Deletes the `agent` row; `repository_agent.agent_id` cascades and `agent_run.agent_id` is set null after `agent_slug` and `agent_description` are snapshotted. | Future `buildPullRequestReviewInput` queries cannot select the deleted agent or its deleted assignments; `getRunInspector` in `applications/web/src/lib/server/review/operator.ts` reads specialist `agent_run` rows and falls back to stored snapshot fields when the agent row is gone. | Deleted agents stop running for future repository automation and disappear from assignment state. Historical specialist `agent_run` rows, findings, events, and cost links remain inspectable after deletion. | `applications/web/src/routes/(authenticated)/agents/[agentId]/agent-detail-page.server.test.ts` covers delete action flow. `packages/database/src/schema/__tests__/agent-deletion-history.test.ts` covers assignment cleanup, database-side snapshot backfill, and preserved run history. `applications/web/src/lib/server/review/operator.test.ts` covers historical inspector access after deletion.                            | Wired   |

## Stored Engine Setting Without Current UI

`repository_review_settings.check_conclusion_mode` is not a current user-facing setting. The schema
default is `advisory`, the only non-default value is `gating`, and the engine path is wired:
`buildPullRequestReviewInput` selects it, `toCheckConclusionMode` normalizes it, and
`buildCompletedCheckRunPatch` turns error-severity findings into a failed Tribunal check run only in
gating mode. Tests in `applications/engine/src/workflows/review-intent-port.test.ts` and
`applications/engine/src/workflows/review-workflow.test.ts` cover both advisory and gating behavior.

Because no UI control writes this field today, it is not a silent no-op. Adding a repository-level
control for it would be a product expansion rather than a defect found by this audit.
