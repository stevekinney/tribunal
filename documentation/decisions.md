# Tribunal Review Engine Decisions

## 2026-06-17 Foundation Dependency Pins

- `@lostgradient/weft`: `0.6.0` from npm and already present in the existing web app.
- `tensorlake`: `0.5.47` from npm.
- `@anthropic-ai/claude-agent-sdk`: `0.3.181` from npm.

The foundation workspaces pin the runtime review-engine dependencies exactly where they are introduced. Later tracks should update this file before changing any of these versions.

## 2026-06-17 Review Intent Idempotency Scope

- `review_intent` idempotency is scoped to `delivery_id`, `kind`, `repository_id`, and `pr_number`.
- GitHub can send one delivery that references more than one pull request for check events. Even though the initial durable contract only enqueues pull request lifecycle events, the database constraint should preserve pull request scope so future check-driven review intents cannot suppress sibling pull requests.
- The webhook deferred-claim filter is intentionally limited to lifecycle events that write a durable `review_intent`: `opened`, `reopened`, `ready_for_review`, `synchronize`, and `closed`. Broader review/comment/check events should only move to deferred claiming when they also persist an explicit intent kind.

## 2026-06-19 Cost Event Agent Run Index

- `cost_event_agent_run_idx` is intentionally retained in addition to the required review-run and user-time indexes.
- The index supports agent-level cost drilldowns from review run inspector views without scanning every cost event for a review run.
- This is a read-path optimization only; it does not change cost-event ownership, tenancy, or reconciliation semantics.

## 2026-07-25 Cost Event Source Read-Path Inventory

This inventory covers runtime reads and decisions that touch `cost_event` or present cost figures
derived from review-run estimate fields. It deliberately excludes the retired Anthropic
cost-reconciliation endpoint and pipeline; per-run reconciliation is not achievable with that
endpoint, and the historical implementation was removed by issue #215 / pull request #219.

| Path                                                                                                                                                                                                               | Source behavior                                                                                                                                                                                                                                             | Label or decision behavior                                                                                                                                                                                                | Verdict                                                                                                   |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- |
| `packages/database/src/queries/review-costs.ts` rollup helpers (`getCostPerReviewRun`, `getCostPerPullRequest`, `getCostPerRepository`, `getCostPerAgent`, `getCostPerAgentPerRepository`, `getCostPerUserPerDay`) | `rollupWhere` always scopes to `cost_event.user_id` and scopes to `cost_event.source` when callers pass `source`. The helpers are exported query primitives, not a user-facing display path, and no runtime caller currently uses the six rollups directly. | Callers that present one of these aggregates must pass the source they are labelling. The existing tests exercise the intended estimate-source contract and prove other users' estimate and reconciled rows are excluded. | Correct when called with an explicit source; no current display or decision path aggregates both sources. |
| `packages/database/src/queries/review-costs.ts` `spendTodayEstimate`                                                                                                                                               | Hard-codes `cost_event.source = 'estimate'` and UTC day bounds for the requested user.                                                                                                                                                                      | Feeds estimate-only daily cap decisions.                                                                                                                                                                                  | Correctly scoped and labelled.                                                                            |
| `packages/cost/src/ledger.ts` `enforceDailyCap` and `createCostPort(...).enforceDailyCap`                                                                                                                          | Calls `spendTodayEstimate`, so reconciled rows cannot affect the cap decision.                                                                                                                                                                              | The settings UI and cost package comments describe the guard as estimated daily spend. Engine callers use this port before the run and before each specialist dispatch.                                                   | Correctly scoped and labelled.                                                                            |
| `applications/web/src/lib/server/review/operator.ts` `getCostOverview`                                                                                                                                             | SQL filters by both `cost_event.user_id` and the explicit `source` argument before computing today totals, rollups, and cache-token totals.                                                                                                                 | The `/costs` route always calls `getCostOverview(user.id, 'estimate')`; the page subtitle and footer label the view as estimated spend, and client tests assert that visible estimate copy.                               | Correctly scoped and labelled.                                                                            |
| Run list page                                                                                                                                                                                                      | Reads `tribunal_run.cost_estimate_usd`, not `cost_event`.                                                                                                                                                                                                   | The table header says `Cost` while the row renders `run.costEstimateUsd`.                                                                                                                                                 | Ambiguous estimate-source label; follow-up issue #255 filed.                                              |
| Run inspector page                                                                                                                                                                                                 | Reads `tribunal_run.cost_estimate_usd` and `agent_run.cost_estimate_usd`, not `cost_event`.                                                                                                                                                                 | Displays the run estimate as `Est. cost`. Engine persistence writes these fields from review workflow estimates.                                                                                                          | Out of direct `cost_event` scope, and label is estimate-specific.                                         |
| Workflow inspector page                                                                                                                                                                                            | Loads recent run rows whose data shape includes `costEstimateUsd`, but does not render the cost field.                                                                                                                                                      | No user-facing cost label or cost decision on the current page.                                                                                                                                                           | Not a current cost display path.                                                                          |
| Completed GitHub Check Run summary                                                                                                                                                                                 | Uses specialist `agentResults` rather than `cost_event`.                                                                                                                                                                                                    | Labels the headline as `Estimated cost`, but omits triage and verification estimates that can be included in `reviewRun.costEstimateUsd` for the completed run.                                                           | Estimate-specific label with an undercounted total; follow-up issue #261 filed.                           |
| `applications/web/test/end-to-end/seed.ts`                                                                                                                                                                         | Test fixture setup and cleanup insert and read `cost_event` rows for seeded repositories.                                                                                                                                                                   | Test-only harness, not a production display or decision path.                                                                                                                                                             | Not a runtime read path.                                                                                  |

Three confirmed follow-ups were filed: #255 tracks the run list presenting `costEstimateUsd` under a
generic `Cost` column, #260 tracks the unused repository 30-day cost rollup read, and #261 tracks
the completed Check Run summary's undercounted estimated cost.
